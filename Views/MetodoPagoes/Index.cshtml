@model IEnumerable<Confecciones.Models.MetodoPago>

@{
    ViewData["Title"] = "Index";
}

<h1>Metodos Pago</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MetodoPago1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MetodoPago1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodMetodoPago">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodMetodoPago">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodMetodoPago">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
